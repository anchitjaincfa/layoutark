export default function P(){return <div className="page"><p className="eyebrow">Local-first</p><h1>Your file list stays with you.</h1><section className="grid section">{[["No uploads","Browser-local analysis only."],["No accounts","No sign-in, analytics, or cloud store."],["Sensitive inventory","Protect manifests containing paths."]].map(x=><article className="card"><h3>{x[0]}</h3><p>{x[1]}</p></article>)}</section></div>}