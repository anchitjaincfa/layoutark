export default function P(){return <div className="page"><p className="eyebrow">Guide 01 / inventory</p><h1>Scan first. Decide second.</h1><p className="lede">The unsigned PowerShell scanner finds .pub files and writes metadata to JSON. It does not open, modify, convert, or upload documents.</p><section className="grid section">{[["Inspect","Read the script first."],["Run locally","Scan authorized folders only."],["Protect JSON","It contains names and paths."]].map(x=><article className="card"><h3>{x[0]}</h3><p>{x[1]}</p></article>)}</section></div>}